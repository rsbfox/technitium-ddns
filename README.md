# technitium-ddns
Dynamic DNS updater for Technitium DNS Server. Reads standard BIND9 zone files and pushes records via the Technitium HTTP API. Supports split-horizon APP records, dry run mode, and automatic WAN IP detection. Designed for self-hosted DNS on a home server with a dynamic IP.
